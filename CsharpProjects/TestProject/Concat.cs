// Concatenación
// Combinar cadenas de texto utilizando el operador de concatenación (+) para crear un mensaje personalizado.

string firstName = "Bob";
string greeting = "Hello";
string message = greeting + " " + firstName + "!";
Console.WriteLine(message);
